apiVersion: v2
name: postgresql-access-operator
description: A Kubernetes operator that manages PostgreSQL databases, roles, and grants on existing PostgreSQL instances
type: application
version: 0.1.0
appVersion: "0.1.0"
