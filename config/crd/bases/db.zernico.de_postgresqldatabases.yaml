---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: postgresqldatabases.db.zernico.de
spec:
  group: db.zernico.de
  names:
    kind: PostgreSQLDatabase
    listKind: PostgreSQLDatabaseList
    plural: postgresqldatabases
    singular: postgresqldatabase
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Database
      type: string
    - jsonPath: .spec.postgresRef.name
      name: PostgreSQL
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PostgreSQLDatabase creates a database on a referenced PostgreSQL
          instance.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PostgreSQLDatabaseSpec defines the desired state of PostgreSQLDatabase.
            properties:
              cleanupPolicy:
                default: Skip
                description: |-
                  cleanupPolicy defines what happens when this CR is deleted.
                  Skip (default): the resource is NOT dropped from PostgreSQL.
                  Delete: the resource IS dropped from PostgreSQL.
                enum:
                - Skip
                - Delete
                type: string
              name:
                description: |-
                  name is the database name to create in PostgreSQL.
                  If empty, defaults to metadata.name.
                type: string
              postgresRef:
                description: postgresRef is a cross-namespace reference to the PostgreSQL
                  instance.
                properties:
                  name:
                    description: name is the name of the PostgreSQL CR.
                    type: string
                  namespace:
                    description: |-
                      namespace is the namespace of the PostgreSQL CR.
                      If empty, defaults to the namespace of the referencing resource.
                    type: string
                required:
                - name
                type: object
              requeueInterval:
                description: |-
                  requeueInterval is the interval for periodic re-reconciliation (drift detection).
                  If not set, the global default (30s) is used.
                type: string
              retryInterval:
                description: |-
                  retryInterval is the interval for retrying on errors.
                  If not set, the resource is requeued immediately on error.
                type: string
            required:
            - postgresRef
            type: object
          status:
            description: PostgreSQLDatabaseStatus defines the observed state of PostgreSQLDatabase.
            properties:
              conditions:
                description: conditions represent the current state of the PostgreSQLDatabase
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
